# Archivo .env de prueba

# Variables de cadena básicas
# Esta sección prueba la lectura de variables de cadena simples, sin comillas ni caracteres especiales.
SIMPLE_STRING=valor_simple
# Prueba una variable de cadena con espacios al principio y al final del valor.
SPACED_STRING=  valor con espacios  
# Prueba una variable de cadena que está definida pero no tiene valor asignado después del signo igual.
EMPTY_STRING=
# Prueba una variable de cadena cuyo valor contiene el signo igual (=).
VALUE_WITH_EQUALS=parte1=parte2=parte3

# Variables de cadena vacías (explícitas e implícitas)
# Esta sección prueba las diferentes formas de definir variables con cadenas vacías.
# Prueba una variable definida con comillas simples y sin contenido dentro, representando una cadena vacía explícita.
EXPLICIT_EMPTY_STRING_SINGLE_QUOTES=''
# Prueba una variable definida con comillas dobles y sin contenido dentro, representando una cadena vacía explícita.
EXPLICIT_EMPTY_STRING_DOUBLE_QUOTES=""
# Prueba una variable definida sin ningún valor después del signo igual, representando una cadena vacía implícita.
IMPLICIT_EMPTY_STRING= # Variable sin valor después del igual
# Prueba una variable definida pero sin valor (o valor vacío implícito) para ser usada en pruebas de "fallback" o valor alternativo.
MISSING_VARIABLE= # Variable definida pero sin valor (para pruebas de fallback)

# Variables entrecomilladas
# Esta sección prueba la lectura de variables de cadena que están rodeadas por comillas simples o dobles.
# Prueba una variable de cadena rodeada de comillas simples.
SINGLE_QUOTES='valor con comillas simples'
# Prueba una variable de cadena rodeada de comillas dobles.
DOUBLE_QUOTES="valor con comillas dobles"

# Variables numéricas
# Esta sección prueba la lectura de variables que representan números enteros y decimales.
# Prueba una variable que representa un número entero positivo.
INT_VALUE=123
# Prueba una variable que representa un número entero negativo.
NEGATIVE_INT=-45
# Prueba una variable que representa un número decimal (punto flotante) positivo.
DOUBLE_VALUE=3.14
# Prueba una variable que representa un número decimal (punto flotante) negativo.
NEGATIVE_DOUBLE=-0.5

# Variables booleanas
# Esta sección prueba la lectura de variables que representan valores booleanos (verdadero o falso).
# Prueba una variable que representa el valor booleano verdadero usando la cadena "true".
BOOL_TRUE=true
# Prueba una variable que representa el valor booleano falso usando la cadena "false".
BOOL_FALSE=false
# Prueba una variable que representa el valor booleano verdadero usando el número "1".
BOOL_ONE=1
# Prueba una variable que representa el valor booleano falso usando el número "0".
BOOL_ZERO=0

# Comentarios
# Esta sección prueba manejar las líneas de comentario.
# Prueba una línea que comienza con el carácter '#', que debería ser ignorada como comentario.
COMMENT_LINE=# Esta es una línea comentada
# Prueba una variable cuyo valor contiene un comentario en línea después del signo igual.
# Aqui, el comentario en línea no se considera parte del valor de la variable.
INLINE_COMMENT=valor con # comentario al final
